[project]
name = "role-extractor"
version = "0.1.0"
description = "Extract role structure from curricular documents (roles.v1 schema)"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.60",
    "jinja2>=3.1",
    "pypdf>=5.0",
    "python-dotenv>=1.0",
]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.uv]
package = false
